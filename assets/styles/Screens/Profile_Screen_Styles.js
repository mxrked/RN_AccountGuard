import { StyleSheet } from "react-native";

const Profile_Screen_Styles = StyleSheet.create({
  profileScreenMainView: {
    flex: 1,
    alignItems: "center",
    justifyContent: "center",
    // backgroundColor: "rgba(0, 0, 0, 0.25)",
  },
});

export default Profile_Screen_Styles;
